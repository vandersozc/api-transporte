package com.vandersoncamp.transporte.repository;import com.vandersoncamp.transporte.model.Endereco;import org.springframework.data.jpa.repository.JpaRepository;import org.springframework.stereotype.Repository;@Repositorypublic interface EnderecoRepository extends JpaRepository<Endereco, Long> {    Iterable<Endereco> findByLogradouro(String logradouro);    Endereco findByCep(String cep);    /**     * @Query("select bean from Endereco bean where cliente.id = :idCliente")     * Iterable<Endereco> findByEnderecoIdCliente(@Param("idCliente") Long idCliente);     */}